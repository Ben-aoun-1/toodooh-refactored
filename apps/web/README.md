# apps/web

React 18 + Vite frontend for the TOODOOH platform.

This directory is currently an empty placeholder. The next step in the migration is to copy
the existing TOODOOH frontend (its `package.json`, `src/`, Vite config, etc.) into this
folder, at which point that `package.json` becomes authoritative and the workspace picks it
up automatically via `pnpm-workspace.yaml`.

Do not add a `package.json` or source files here until that copy step happens.
